{
    "build": {
        "context": "",
        "Dockerfile": "Dockerfile"
    },
    "run": {
        "env": {
            "MY_ENV": "hello world"
        },
        "mountpoints": [
            {
                "mount_type": "bind",
                "mountpoint": "/simplex/inputs/inputs.txt",
                "read_only": true,
                "required": true
            },
            {
                "mount_type": "bind",
                "mountpoint": "/simplex/outputs/outputs.txt",
                "read_only": false,
                "required": true
            }
        ]
    }
}
